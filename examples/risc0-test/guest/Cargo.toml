[package]
name = "clvm-risc0-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm = "3.0.3"

# Use clvm_tools_rs in no_std mode (no default features)
clvm_tools_rs = { path = "../../..", default-features = false }

# Serialization
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
borsh = { version = "1.5.7", features = ["derive"] }

# RISC0 accelerated SHA256
sha2 = { git = "https://github.com/risc0/RustCrypto-hashes", tag = "sha2-v0.10.8-risczero.0" }

# BLS signature verification with RISC0 precompiles
bls12_381 = { git = "https://github.com/risc0/zkcrypto-bls12_381.git", default-features = false, features = ["groups", "pairings", "experimental"] }

# ECDSA signature verification
k256 = { version = "0.13", default-features = false, features = ["ecdsa"] }
